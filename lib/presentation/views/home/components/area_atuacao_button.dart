import 'package:flutter/material.dart';

class AreaAtuacaoButton extends StatelessWidget {
  const AreaAtuacaoButton({super.key});

  @override
  Widget build(BuildContext context) {
    return InkWell(
      onTap: (){},
      child: Container(),
    );
  }
}